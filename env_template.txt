QUICKBOOKS_CLIENT_ID=your_client_id_here
QUICKBOOKS_CLIENT_SECRET=your_client_secret_here
QUICKBOOKS_REFRESH_TOKEN=your_refresh_token_here
QUICKBOOKS_COMPANY_ID=your_company_id_here
QUICKBOOKS_ENV=sandbox

# MCP Transport Settings
# stdio = local Claude Desktop (default), sse = remote HTTP access
MCP_TRANSPORT=stdio
# Host and port for SSE mode (uses FastMCP settings prefix)
FASTMCP_HOST=0.0.0.0
FASTMCP_PORT=8000